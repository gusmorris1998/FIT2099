package game.interfaces;

public interface EnemyDetails {
    /**
     * Returns the number of souls that a player would gain upon killing a specific enemy
     *
     */
    int getSouls();
    int getHP();
}
